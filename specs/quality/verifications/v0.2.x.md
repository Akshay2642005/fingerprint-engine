# Verified milestones — v0.1.0 … v0.2.2

Machine gate for every release: `zig build test --summary all` green.

## v0.1.0 / v0.1.1 (2026-07-11)

- [x] `zig build test` green.
- [x] `zig build wasm` produces `fingerprint.wasm`; `zig build native`
      produces the static library (pre-rework surface).
- [x] Browser demo page collects and displays a 102-signal fingerprint.
- [x] npm `@fingerprint/sdk` v0.1.x installs and runs (WASM-era package).
- [x] UAT: user confirmed the demo output (digest + signals) on their machine.

## v0.2.0 (2026-08-07) — breaking rework milestone

- [x] `zig build test` green on Zig 0.14.1 after the downgrade commit.
- [x] Repo reorganized; `src/server/` gone; no `zig build native`.
- [x] `zig build scripts -- help` and `zig build docs` work.
- [x] `zig build clients:browser` produces the npm `dist/` via Zig.
- [x] UAT: user confirmed the reworked layout and build surface.

## v0.2.1 (2026-08-08)

- [x] Worker executable e2e: `zig build test-integration` green (loopback pipe
      + tcp request/response against the pinned digest).
- [x] `zig build scripts -- worker request` round-trips against a running
      worker (tcp) and prints a digest matching the local engine.
- [x] Live RabbitMQ: `zig build scripts -- amqp` connects to compose's
      RabbitMQ 4, declares the `fingerprint` exchange, round-trips a frame.
- [x] Docker: `zig build docker:worker` builds; `docker run --rm -p 8080:8080
      fingerprint-worker:0.2.0` serves frames on 8080.
- [x] Browser demo (`examples/demo.html`) collects signals and POSTs them to a
      local ingress URL.
- [x] npm package published (manual) and installs; dist surface guard passes.
- [x] UAT: user confirmed container and demo behavior on their machine.

## v0.2.2 (2026-08-08)

- [x] `zig build test --summary all` → 383/383 (376 unit + 7 integration).
- [x] `zig build wasm` green (infra artifact).
- [x] `zig build scripts -- worker request` → same pinned digest on repeat
      runs (determinism/replay evidence):
      `digest: db29fc13…e6c75 (matches local engine)`, features=3, schema=2.
- [x] GHCR image pushed with lowercase tags:
      `ghcr.io/akshay2642005/fingerprint-engine/fingerprint-worker:0.2.2`
      and `:latest` (verified from the release pipeline logs).
- [x] npm v0.2.2 published automatically via the OIDC trusted publisher
      (no NPM_TOKEN) — verified in `publish-npm` job logs.
- [x] GitHub Release v0.2.2 created with the WASM artifact.
- [x] Live AMQP inspection (session evidence, `src/scripts.zig` `amqp get`):
      `zig build scripts -- amqp get --count=1 --timeout-ms=60000` +
      `zig build scripts -- worker request` (other terminal) →
      `frame: type=fingerprint_result codec=binary payload=37 bytes
      integrity=OK`, `digest: db29fc…e6c75`, features=3 schema=2; RabbitMQ
      4.3.4 management UI confirmed published messages with
      `fpkg-message-type: fingerprint-result` headers and persistent
      delivery_mode=2; worker logged `basic_ack delivery_tag=1/2`.
- [x] `zig build scripts -- worker request` repeated → identical digest
      (idempotent replay through the live broker path).
- [x] UAT: user confirmed the AMQP publish/ack inspection.

## Known-good fixtures (do not regenerate)

- `tests/fixtures/fingerprints/signal-package-v2.bin` — pins
  `sdk_version "0.2.0"` and the digest constant
  `db29fc13d8dad5dc0bd7b1f997155cff411f3da88a597619f5e0d67a251e6c75`
  (features=3, schema=2), referenced as a compile-time constant in
  `src/integration_tests.zig` and consumed by the TS parity test via
  `signal-package-v2.signals.json`.
