[package]
name = "fingerprint-sdk"
version = "0.1.0"
description = "Fingerprint Engine — Rust bindings for browser fingerprint matching"
authors = ["Fingerprint Engine Contributors"]
license = "MIT"
edition = "2021"
repository = "https://github.com/your-org/fingerprint-engine"
keywords = ["fingerprint", "browser-fingerprinting", "wasm", "security"]
categories = ["wasm", "cryptography", "web-programming"]

[package.metadata.docs.rs]
features = ["std"]

[features]
default = ["std"]
std = []

[dependencies]
libc = "0.2"

[build-dependencies]
